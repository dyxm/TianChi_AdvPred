# Created by [Yuexiong Ding] at 2018/3/16
# 计算两向量的相似度（采用预先相似度）
#
# from sklearn.feature_extraction.text import TfidfVectorizer

# 我们建一个TfidfVectorizer实例来计算TF-IDF权重：
# vectorizer=TfidfVectorizer()
# X_train=vectorizer.fit_transform(X_train_raw)
# X_test=vectorizer.transform(X_test_raw)


class Similarity:
    def __init__(self, item_category, item__property, predict_category_property):
        # it_category = item_category
        # it__property = item__property
        # pred_category_property = predict_category_property
        pass

    def to_vector(self, raw_data):
        pass
